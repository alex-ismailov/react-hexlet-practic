import ReactDOM from 'react-dom';
import React from 'react';

// BEGIN (write your solution here)
import Card from './Card.jsx';

const mount = document.getElementById('container');
ReactDOM.render(<Card />, mount);
// END
